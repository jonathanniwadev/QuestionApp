<Page
    x:Class="QuestionApp.SignUpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuestionApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  

            <Grid Padding="20">
                <TextBlock FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,20" Text="Sign Up" />

                <TextBox x:Name="newUsernameBox" PlaceholderText="Username" FontSize="20" Margin="0,100,0,20" />

                <PasswordBox x:Name="newPasswordBox" PlaceholderText="Password" FontSize="20" Margin="0,150,0,20" />

                <PasswordBox x:Name="confirmPasswordBox" PlaceholderText="Confirm Password" FontSize="20" Margin="0,200,0,20" />

                <Button Content="Sign Up" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="20" Margin="0,250,0,0" Width="200" Click="SignUpButton_Click"/>

                <TextBlock FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,300,0,0">
            Already have an account? <Hyperlink Click="LoginLink_Click">Login</Hyperlink>
                </TextBlock>
            </Grid>
        </Page>


 